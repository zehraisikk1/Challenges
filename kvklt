#include <stdio.h>
#include <string.h>

int main() {
    char sentence[600];
    printf("Enter a sentence: ");
    scanf("%[^\n]s", sentence);
    printf("Your sentence is: %s", sentence);
    return 0;
}

